version: "3.9"

services:
  postgres:
    image: postgres:15
    container_name: gitpulse_postgres
    restart: always
    environment:
      POSTGRES_USER: gitboard_user
      POSTGRES_PASSWORD: gitboard_pass
      POSTGRES_DB: gitboard
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7
    container_name: gitboard_redis
    restart: always
    ports:
      - "6379:6379"

volumes:
  postgres_data: